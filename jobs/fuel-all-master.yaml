- job:
    name: fuel-all-master
    logrotate:
      daysToKeep: 10
      numToKeep: 10
      artifactDaysToKeep: -1
      artifactNumToKeep: 1
    scm:
      - git:
          url: https://github.com/stackforge/fuel-main.git
          branches:
            - master
          skip-tag: true
          wipe-workspace: false
    builders:
      - copyartifact:
          project: fuel-artifacts-5.0.x
          filter: artifacts/*
          target: objects/5.0.x
          flatten: true
      - shell: |
          git review -x 109569
          rm -rf artifacts
          make deep_clean
          make DEPS_DIR=objects ARTS_DIR=artifacts USE_MIRROR=msk EXTRA_RELEASES=5.0.x all
    publishers:
      - archive:
          artifacts: artifacts/*
          latest-only: true
    triggers:
      - timed: H H(6-7) * * 1-5
    wrappers:
      - timeout:
          timeout: 180
          fail: true
