- job:
    name: fuel-artifacts-master
    logrotate:
      daysToKeep: 10
      numToKeep: 10
      artifactDaysToKeep: -1
      artifactNumToKeep: 1
    scm:
      - git:
          url: https://github.com/stackforge/fuel-main.git
          branches:
            - master
          skip-tag: true
          wipe-workspace: false
    builders:
      - shell: |
          git review -x 107730
          git review -x 107866
          rm -rf artifacts
          make deep_clean
          make ART_DIR=artifacts USE_MIRROR=msk artifacts
    publishers:
      - archive:
          artifacts: artifacts/*
          latest-only: true
    triggers:
      - timed: #H H(3-5) * * 1-5
    wrappers:
      - timeout:
          timeout: 90
          fail: true
